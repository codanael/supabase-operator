---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: supabasetenants.supabase.codanael.io
spec:
  group: supabase.codanael.io
  names:
    kind: SupabaseTenant
    listKind: SupabaseTenantList
    plural: supabasetenants
    singular: supabasetenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantId
      name: Tenant ID
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SupabaseTenant is the Schema for the supabasetenants API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SupabaseTenantSpec defines the desired state of a SupabaseTenant.
            properties:
              auth:
                description: TenantAuthSpec configures the GoTrue auth service for
                  a tenant.
                properties:
                  additionalRedirectURLs:
                    items:
                      type: string
                    type: array
                  disableSignup:
                    type: boolean
                  email:
                    description: EmailAuthSpec configures email-based authentication.
                    properties:
                      autoconfirm:
                        type: boolean
                      enabled:
                        default: true
                        type: boolean
                    type: object
                  external:
                    description: OAuthProviders configures external OAuth providers.
                    properties:
                      azure:
                        description: OAuthProvider configures a single OAuth provider.
                        properties:
                          credentialsSecret:
                            description: CredentialsSecret is the name of a Secret
                              containing "clientId" and "clientSecret" keys.
                            type: string
                          enabled:
                            type: boolean
                        required:
                        - credentialsSecret
                        - enabled
                        type: object
                      github:
                        description: OAuthProvider configures a single OAuth provider.
                        properties:
                          credentialsSecret:
                            description: CredentialsSecret is the name of a Secret
                              containing "clientId" and "clientSecret" keys.
                            type: string
                          enabled:
                            type: boolean
                        required:
                        - credentialsSecret
                        - enabled
                        type: object
                      google:
                        description: OAuthProvider configures a single OAuth provider.
                        properties:
                          credentialsSecret:
                            description: CredentialsSecret is the name of a Secret
                              containing "clientId" and "clientSecret" keys.
                            type: string
                          enabled:
                            type: boolean
                        required:
                        - credentialsSecret
                        - enabled
                        type: object
                    type: object
                  siteURL:
                    type: string
                  smtp:
                    description: SMTPSpec configures SMTP for sending auth emails.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret is the name of a Secret containing
                          "username" and "password" keys.
                        type: string
                      host:
                        type: string
                      port:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      senderName:
                        type: string
                    required:
                    - credentialsSecret
                    - host
                    - port
                    type: object
                type: object
              functions:
                description: TenantFunctionsSpec configures the Edge Functions runtime.
                properties:
                  source:
                    description: FunctionSource defines where edge function code is
                      loaded from.
                    properties:
                      configMapRef:
                        description: ConfigMapRef is the name of a ConfigMap containing
                          function source code.
                        type: string
                    required:
                    - configMapRef
                    type: object
                  verifyJWT:
                    default: true
                    type: boolean
                type: object
              realtime:
                description: TenantRealtimeSpec configures the Realtime service.
                properties:
                  enabled:
                    default: true
                    type: boolean
                  replicas:
                    default: 1
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              resources:
                default: small
                description: Resources selects a resource sizing preset for tenant
                  workloads.
                enum:
                - small
                - medium
                - large
                - custom
                type: string
              rest:
                description: TenantRESTSpec configures the PostgREST API service.
                properties:
                  maxRows:
                    default: 1000
                    format: int32
                    type: integer
                  schemas:
                    default:
                    - public
                    - graphql_public
                    items:
                      type: string
                    type: array
                type: object
              storage:
                description: TenantStorageSpec configures the Storage service.
                properties:
                  backend:
                    default: file
                    description: StorageBackend defines the backend type for tenant
                      storage.
                    enum:
                    - file
                    - s3
                    - obc
                    type: string
                  fileSizeLimit:
                    default: 52428800
                    format: int64
                    type: integer
                  imageTransformation:
                    type: boolean
                  objectBucket:
                    description: ObjectBucketSpec configures OBC-based storage using
                      the ObjectBucketClaim API.
                    properties:
                      additionalConfig:
                        additionalProperties:
                          type: string
                        type: object
                      bucketPrefix:
                        type: string
                      storageClassName:
                        type: string
                    type: object
                  s3:
                    description: S3Config configures S3-compatible storage.
                    properties:
                      bucket:
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret is the name of a Secret containing
                          "accessKeyId" and "secretAccessKey" keys.
                        type: string
                      endpoint:
                        type: string
                      forcePathStyle:
                        type: boolean
                      region:
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - region
                    type: object
                type: object
              supabaseRef:
                description: SupabaseRef is the name of the parent Supabase resource
                  (in the same namespace).
                type: string
              suspended:
                description: Suspended, when true, scales tenant workloads to zero.
                type: boolean
              tenantId:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z][a-z0-9-]*[a-z0-9]$
                type: string
            required:
            - supabaseRef
            - tenantId
            type: object
          status:
            description: SupabaseTenantStatus defines the observed state of SupabaseTenant.
            properties:
              components:
                description: TenantComponentStatuses tracks readiness of each tenant
                  component.
                properties:
                  auth:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  database:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  functions:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  realtime:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  rest:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  routing:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                  storage:
                    properties:
                      message:
                        type: string
                      ready:
                        type: boolean
                      version:
                        type: string
                    required:
                    - ready
                    type: object
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoint:
                type: string
              namespace:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
